# noxif

A simple tool to strip EXIF metadata from images, built with Nuxt 4 and Piexifjs. Runs entirely in the browser, ensuring your images never leave your device.

Works with:

- JPG
- JPEG
- TIFF

## Setup

Install deps:

```bash
bun i
```

Run dev server at: `http://localhost:3000`

```bash
bun run dev
```
